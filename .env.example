# Vercel Postgres Database
# Get this from Vercel dashboard after creating a Postgres database
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# JWT Secret for authentication
# Generate a strong random secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environment
NODE_ENV=production

# Frontend API URL (optional, defaults handled in code)
# Leave empty for Vercel deployment
REACT_APP_API_URL=
