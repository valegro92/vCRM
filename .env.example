# Neon Postgres Database (from Vercel Marketplace)
# Get this from Neon dashboard: https://console.neon.tech
# Example: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=

# JWT Secret for authentication
# Generate a strong random secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environment
NODE_ENV=production

# Frontend API URL (optional, defaults handled in code)
# Leave empty for Vercel deployment
REACT_APP_API_URL=
